# Core dependencies (always required by this pack)
numpy>=1.24
sounddevice>=0.4.6
soundfile>=0.12
huggingface_hub>=0.20
# MP3 encoder for progressive streaming to clients. PyPI ships precompiled
# wheels that bundle libmp3lame so no system library installation is required.
# Used to encode PCM chunks into MP3 frames on the fly for the
# /audio/{message_id}/stream endpoint (browsers reject progressive WAV with
# 0xFFFFFFFF header on iOS Safari / some Chromes).
lameenc>=1.5
# Irodori-TTS は ref_wav のロードに torchaudio.load を使う。最近の torch
# (>=2.10 系) は backend に torchcodec を要求するため明示的にインストール。
# GPT-SoVITS のみを使う場合は不要だが、両対応にしておく。
torchcodec>=0.10

# TTS backends are installed separately by setup.bat / install_backends.py.
# Each backend clones its upstream repository into external/ and (where
# applicable) runs `pip install -e .` on it. Backend-specific Python deps
# (torch, transformers, etc.) are pulled in by each upstream's setup.
#
# Preferred entry point (Windows):
#   setup.bat                    # installs gpt_sovits by default
#   setup.bat irodori            # experimental
#   setup.bat all
#
# Direct invocation (cross-platform):
#   python scripts/install_backends.py gpt_sovits   # recommended
#   python scripts/install_backends.py irodori
#   python scripts/install_backends.py all
